/**
 * Form model for updating an existing lyrics record's text content.
 *
 * @interface IUpdateLyricsCredentials
 * @property {string} lyricsText - Updated lyrics text content (required)
 */
export interface IUpdateLyricsCredentials {
    lyricsText: string;
}
