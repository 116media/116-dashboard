/**
 * Form model for attaching a pricing tier to an order item.
 *
 * @interface IAddItemTierCredentials
 * @property {string} pricingTierId - Pricing tier UUID
 */
export interface IAddItemTierCredentials {
    pricingTierId: string;
}
