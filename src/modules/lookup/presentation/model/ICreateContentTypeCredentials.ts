/**
 * Form model for creating a new content type.
 *
 * @interface ICreateContentTypeCredentials
 * @property {string} name - Content type name (required, max 50 chars)
 */
export interface ICreateContentTypeCredentials {
    name: string;
}
