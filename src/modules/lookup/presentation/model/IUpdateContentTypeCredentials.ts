/**
 * Form model for updating an existing content type.
 *
 * @interface IUpdateContentTypeCredentials
 * @property {string} name - Content type name (required, max 50 chars)
 */
export interface IUpdateContentTypeCredentials {
    name: string;
}
