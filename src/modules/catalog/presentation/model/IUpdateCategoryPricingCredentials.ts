/**
 * Form model for updating a category pricing entry.
 *
 * @interface IUpdateCategoryPricingCredentials
 * @property {number} priceUsd - Updated price in USD (required)
 */
export interface IUpdateCategoryPricingCredentials {
    priceUsd: number;
}
